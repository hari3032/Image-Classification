# Image-Classification
This project implements an image classification system using Google Teachable Machine and TensorFlow.js. The model is trained with custom images and can classify live webcam input into predefined categories. The application runs completely in the browser without requiring any backend server.  
